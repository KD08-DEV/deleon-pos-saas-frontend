export const QK = {
    ADMIN_FISCAL_CONFIG: ["admin-fiscal-config"],
    TENANT_CONFIG: ["tenant-config"],

    // Mesas
    TABLES: ["tables"],

    // Órdenes (por si lo necesitas luego)
    ORDERS: ["orders"],
};
